{
    "name": "multiverse-wip",
    "version": "1.0.0",
    "description": "Multiverse testing for the robots",
    "main": "src/index.ts",
    "scripts": {
        "test": "jest",
        "clean": "rm -rf dist",
        "build": "tsc -p . && mv dist/src/* dist && rm -r dist/src && rm dist/package.json",
        "start": "node -r dist/index.js",
        "dev": "nodemon --exec 'ts-node' -r src/index.ts"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {},
    "devDependencies": {
        "@types/jest": "^26.0.23",
        "jest": "^27.0.3",
        "ts-jest": "^27.0.2",
        "ts-node": "^10.0.0",
        "typescript": "^4.3.2"
    }
}
